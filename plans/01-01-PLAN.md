---
phase: 01-jev-doc-parser
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [typesafe-doc-parser/backend/corpus/build_corpus.py, typesafe-doc-parser/backend/corpus/corpus.json]
autonomous: true
requirements: [DOC-01]
user_setup: []

must_haves:
  truths:
    - "Every docs page from llms.txt is stored as heading/subheading blocks with stable IDs"
    - "No block list given to Jev ever exceeds 255 options (windowing metadata exists)"
    - "A developer can look up any block ID and get its page URL + heading breadcrumb + verbatim text"
  artifacts:
    - path: "typesafe-doc-parser/backend/corpus/build_corpus.py"
      provides: "llms.txt fetch + .md fetch + heading split + ID tagging"
      min_lines: 80
    - path: "typesafe-doc-parser/backend/corpus/corpus.json"
      provides: "Parsed corpus: pages, blocks, windows"
  key_links:
    - from: "typesafe-doc-parser/backend/corpus/build_corpus.py"
      to: "https://docs.typesafe.ai/llms.txt"
      via: "fetch index then per-page .md"
      pattern: "llms\\.txt"
---

<objective>
Ingest TypeSafe docs end-to-end and parse to heading/subheading blocks with stable IDs.

Purpose: Jev can only point at IDs we give it (semantic_find pattern). Bad chunking = bad answers.
Output: build_corpus.py + corpus.json
</objective>

<execution_context>
@/Users/sarnavo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/sarnavo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@typesafe-doc-parser/00-FEASIBILITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build corpus ingester (llms.txt to tagged blocks)</name>
  <files>typesafe-doc-parser/backend/corpus/build_corpus.py</files>
  <action>Write Python script (stdlib + requests only, NO LLM/embedding deps): fetch https://docs.typesafe.ai/llms.txt, parse markdown links to page paths, fetch each page's .md variant (append .md), split markdown by ATX headings (#/##/###) into blocks keeping {block_id, page_url, page_title, heading_path (breadcrumb list), level, text}. Block IDs: H-prefixed per page with zero-padded index (e.g. introduction-H004). Preserve code fences verbatim (do not split inside ``` blocks). Write corpus.json {pages:[...], blocks:[...], built_at, source:"llms.txt"}. NO typesafe-sdk calls in this task (pure parsing). Log counts per page to stdout.</action>
  <verify>python3 typesafe-doc-parser/backend/corpus/build_corpus.py && python3 -c "import json; c=json.load(open('typesafe-doc-parser/backend/corpus/corpus.json')); print(len(c['pages']), len(c['blocks'])); assert len(c['pages'])>=10, 'too few pages'; assert all(b['block_id'] and b['text'].strip() and b['heading_path'] for b in c['blocks']), 'bad blocks'"</verify>
  <done>corpus.json exists with >=10 pages, every block has block_id + heading_path + non-empty verbatim text</done>
</task>

<task type="auto">
  <name>Task 2: Add window map for the 255-option Choice cap</name>
  <files>typesafe-doc-parser/backend/corpus/build_corpus.py, typesafe-doc-parser/backend/corpus/corpus.json</files>
  <action>Extend the same script: group blocks into overlapping windows of max 200 blocks with 20-block overlap (cookbook two-pass pattern: Choice-1 picks window, Choice-2 ranks inside). Store windows:[{window_id, block_ids}] in corpus.json. Add --check flag that fails if any page alone exceeds 255 blocks without windows, and prints largest page/window sizes. Keep everything stdlib-only.</action>
  <verify>python3 typesafe-doc-parser/backend/corpus/build_corpus.py --check && python3 -c "import json; c=json.load(open('typesafe-doc-parser/backend/corpus/corpus.json')); assert all(len(w['block_ids'])<=200 for w in c['windows']), 'window too big'; print('windows:', len(c['windows']))"</verify>
  <done>corpus.json contains windows, every window <=200 blocks, --check passes</done>
</task>

</tasks>

<verification>
Rerun ingester twice: block counts identical (deterministic IDs). Spot-check 3 blocks: text matches live .md source exactly.
</verification>

<success_criteria>
corpus.json covers the docs index, blocks carry heading breadcrumbs, windowing guarantees no Jev Choice ever exceeds 255 options
</success_criteria>

<output>
After completion, create `typesafe-doc-parser/plans/01-01-SUMMARY.md`
</output>
