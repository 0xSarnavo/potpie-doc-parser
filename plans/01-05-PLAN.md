---
phase: 01-jev-doc-parser
plan: 5
type: execute
wave: 2
depends_on: [01-04]
files_modified: [typesafe-doc-parser/backend/search/eval_gold.json, typesafe-doc-parser/backend/search/eval.py, typesafe-doc-parser/backend/search/jev_search.py, typesafe-doc-parser/backend/search/tune.py]
autonomous: true
requirements: [DOC-02, DOC-04]
user_setup:
  - service: typesafe
    why: "Tune sweeps run live Jev calls; executor cannot create a key"
    env_vars:
      - name: TYPESAFE_API_KEY
        source: "Already provisioned for this project"

must_haves:
  truths:
    - "A page-derived eval set (>=36 queries: per-page answerables + keyword queries + unanswerables + partials) exists as a checked-in JSON file"
    - "Verdict accuracy >=95% on the eval set with 100% abstention on unanswerables, or residual misses documented with reasons"
    - "Every known failure point has a fix or a documented stop-reason; still 1 system_one call per ask, zero new deps"
  artifacts:
    - path: "typesafe-doc-parser/backend/search/eval_gold.json"
      provides: "Page-derived gold question set with expected verdicts + blocks"
    - path: "typesafe-doc-parser/backend/search/eval.py"
      provides: "Local BM25 recall report + live verdict sweep runner with budgets"
      min_lines: 80
  key_links:
    - from: "typesafe-doc-parser/backend/search/eval.py"
      to: "typesafe-doc-parser/backend/search/jev_search.py"
      via: "import find() + bm25_shortlist()"
      pattern: "jev_search import"
---

<objective>
Deep fine-tune to a measured goal: page-derived question set, keyword recall fixes, iterate (max 3 sweeps) until verdict >=95% + abstain 100%. Failure points cataloged with fix-or-stop-reason each.

Purpose: 11/12 on 12 hand queries is not proof. Proof is a wide page-derived set with keyword variants pushed to goal.
Output: eval_gold.json + eval.py + minimal jev_search.py fixes + 01-05-SUMMARY.md
</objective>

<execution_context>
@/Users/sarnavo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/sarnavo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@typesafe-doc-parser/00-FEASIBILITY.md
@typesafe-doc-parser/backend/search/jev_search.py
@typesafe-doc-parser/backend/search/tune.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page-derived eval set + local keyword recall report (no Jev)</name>
  <files>typesafe-doc-parser/backend/search/eval_gold.json, typesafe-doc-parser/backend/search/eval.py</files>
  <action>Build eval_gold.json deterministically (a script or documented manual list, checked in): >=36 queries covering (a) one answerable per major page family (concepts, primitives x3, confidence, how-to-build, patterns x2, cookbooks x4 incl. semantic_find/rerank/autoformat, sdk python+javascript, api, models) each with expected top block_id; (b) keyword-style variants (2-4 words, no question form, e.g. "rerank accuracy", "rate limit handling", "jev image support") with expected page fragment; (c) all 4 existing unanswerables + 2 more; (d) 3-4 partials incl. the firing image-input one. eval.py runs LOCAL-ONLY recall first: for each answerable/keyword query report BM25 rank of expected block (top-30 hit or MISS) — zero Jev calls. Known weak spots to include: "reranking" vs "Re-ranking", "rate limits" phrasing, tiny alias pages (LOG_LEVELS, ScoreCriteria).</action>
  <verify>python3 typesafe-doc-parser/backend/search/eval.py --local-only (prints recall table: BM25 top-30 hit rate; lists every MISS with query + expected block)</verify>
  <done>eval_gold.json checked in with >=36 queries; local recall table shows exactly which queries BM25 misses</done>
</task>

<task type="auto">
  <name>Task 2: Iterate to goal — failure points with fix-or-stop, max 3 sweeps</name>
  <files>typesafe-doc-parser/backend/search/jev_search.py, typesafe-doc-parser/backend/search/eval.py</files>
  <action>Work this failure-point table in order, smallest fix first, stop at first rung that holds. Live budget: max 3 full sweeps (~36 calls each, ~110 total incl. probes). STOP after 3 sweeps regardless; document residuals.
  FP1 BM25 miss on compounds/hyphens (reranking vs re-ranking): fix = tokenizer normalization (split on hyphens already; add light suffix-strip stemmer for plurals/gerunds, stdlib only). Verify locally before any Jev call.
  FP2 Synonym gap (price/cost, fast/latency): fix = heading_path tokens weighted 2x in BM25 (headings carry intent) + tiny hand alias map (<=15 entries, documented). No embeddings.
  FP3 exists/fully band edges: fix = only criteria rewording if a sweep shows systematic band error; never move FOUND/ABSENT without sweep evidence.
  FP4 Router wrong page but rank right: no fix — rank is extract source of truth; record as display-only.
  FP5 Choice dilution over 30 options (low top-1, good top-3): no fix — track top-3 rank metric; do not chase top-1.
  After fixes: run live sweep (eval.py --live), record verdict accuracy / abstain rate / top-1 / top-3. Goal: verdict >=95% AND abstain 100%. If goal unmet after sweep 3, freeze and document each residual miss with reason (gold error vs model limit).</action>
  <verify>TYPESAFE_API_KEY=xxx python3 typesafe-doc-parser/backend/search/eval.py --live (prints verdict accuracy, abstain rate, top-1/top-3; goal line PASS/FAIL)</verify>
  <done>Goal met, or 3 sweeps exhausted with every residual miss documented with reason</done>
</task>

<task type="auto">
  <name>Task 3: Lock results, audit, summary</name>
  <files>typesafe-doc-parser/backend/search/tune.py, typesafe-doc-parser/plans/01-05-SUMMARY.md</files>
  <action>Fold final thresholds/criteria back so tune.py still passes its gate; re-run no-LLM audit (clean); confirm 1 system_one per ask unchanged and no new imports; write 01-05-SUMMARY.md with: final scores vs goal, failure-point table (fix or stop-reason each), total live calls spent, residual risks for the website layer.</action>
  <verify>grep -rE '^(import|from) (openai|anthropic|transformers|sentence_transformers|langchain|llama_index)' typesafe-doc-parser/backend && exit 1 || echo AUDIT-CLEAN; TYPESAFE_API_KEY=xxx python3 typesafe-doc-parser/backend/search/tune.py | tail -3</verify>
  <done>Audit clean, tune gate passes, SUMMARY written with scores + failure table + spend</done>
</task>

</tasks>

<verification>
eval_gold.json >=36 queries checked in; local recall 100% top-30 after FP1/FP2 (or misses listed); live verdict >=95% + abstain 100% (or residuals documented); spend <= ~120 live calls for the whole plan; 1 call/ask intact.
</verification>

<success_criteria>
Measured goal reached or 3-sweep stop with documented residuals; failure table complete; audits green
</success_criteria>

<output>
After completion, create `typesafe-doc-parser/plans/01-05-SUMMARY.md`
</output>
