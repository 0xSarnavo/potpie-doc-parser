---
phase: 01-jev-doc-parser
plan: 3
type: execute
wave: 3
depends_on: [01-05]
files_modified: [typesafe-doc-parser/web/index.html, typesafe-doc-parser/web/app.js, typesafe-doc-parser/web/styles.css]
autonomous: false
requirements: [DOC-03, DOC-04]
user_setup: []

must_haves:
  truths:
    - "Empty state matches reference: dark centered hero + pill composer + short first message + 3 suggestion chips"
    - "Thread shows user/assistant messages with verdict badge, verbatim lead extract, collapsible sources, marks, copy button"
    - "Doubt is conversational: partial asks one clarifying question with did-you-mean chips, absent states closest section with rephrase chips, never a dead-end or fake answer"
    - "Message states honest: queued pulse (no fake progress), per-cause errors with retry, abort while pending, no keys/LLM in browser"
  artifacts:
    - path: "typesafe-doc-parser/web/index.html"
      provides: "Hero empty state + thread + docked composer layout"
      min_lines: 80
    - path: "typesafe-doc-parser/web/app.js"
      provides: "Chat state machine: ask/render/doubt-chips/errors/abort/persist"
      min_lines: 150
    - path: "typesafe-doc-parser/web/styles.css"
      provides: "Dark chat theme, badges, bars, chips, responsive, reduced-motion"
      min_lines: 120
  key_links:
    - from: "typesafe-doc-parser/web/app.js"
      to: "POST /ask"
      via: "fetch with AbortController on send"
      pattern: "fetch.*/ask"
---

<objective>
Build the chat website: dark hero empty state like the reference, thread UI, doubt-handling as conversation. Jev-only — all assistant wording is fixed templates + verbatim extracts, zero generation.

Purpose: Best-practice chat UX (SetProduct/Fuselab 2026 patterns) adapted to extractive answers: citations-as-receipts, confidence display, recovery over dead-ends, honesty about uncertainty.
Output: web/ frontend wired to backend /ask
</objective>

<execution_context>
@/Users/sarnavo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/sarnavo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@typesafe-doc-parser/00-FEASIBILITY.md
@typesafe-doc-parser/backend/api/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat UI — hero, thread, doubt flows, honest states</name>
  <files>typesafe-doc-parser/web/index.html, typesafe-doc-parser/web/app.js, typesafe-doc-parser/web/styles.css</files>
  <action>Static frontend, no build step, no libs, no keys. DARK theme like reference (near-black bg, centered hero "Where should we begin?", pill composer).
  EMPTY STATE: first assistant message, short (max ~240 chars): "Ask me anything about the TypeSafe docs. I answer with exact quotes and show my sources — and if something isn't in the docs, I'll say so instead of guessing." + 3 suggestion chips: "What is a System One model?" / "How do I install the Python SDK?" / "What is confidence?". Chips fill composer + submit on click. One-line boundary note under composer ("Answers come only from docs.typesafe.ai · Jev-powered, no LLM").
  THREAD: flat full-width messages, max-width 768px, user right-distinct. Assistant message = verdict badge (answered green / partial amber / not-in-docs red) + lead verbatim extract in blockquote with source link (page_url, new tab) + breadcrumb + <details> "Sources" with next 2-4 ranked blocks (bar width=prob*100 + numeric) + router line ("Best section: X · confidence Y") + copy button (transient ✓) + tiny "jev" model label.
  DOUBT FLOWS (frontend templates, Jev-only intact): partial → after extract, one clarifying line + did-you-mean chips from ranked[1..3] headings (clicking chip asks "Tell me about <heading>"); absent → "That's not in the TypeSafe docs I can see." + "Closest section: <router.choice>" + 2 rephrase chips (top ranked headings). Never "I didn't understand", never invented content.
  STATES: queued = assistant placeholder with pulsing dots labeled "Searching docs…" (NO fake progress %); error = cause-specific line (network/timeout/422-too-short/502) + single Retry button that resends last query; abort = Stop button while pending via AbortController, keeps nothing partial (extractive = all-or-nothing). Optimistic user message render on submit.
  COMPOSER: autogrow textarea (max ~5 rows then scroll), Enter=send / Shift+Enter=newline, send button, live char count near 500 max, inline hint if <3 chars. API base in CONFIG const (default http://localhost:8123).
  SCROLL/A11Y/MOBILE: auto-scroll only if viewport within 100px of bottom; focus stays in composer after send/complete; assistant region aria-live="polite"; message actions after text in DOM; prefers-reduced-motion kills pulse/scroll animation; AA contrast on dark; docked (not floating) composer with safe-area padding; thread persisted to sessionStorage and restored on reload.</action>
  <verify>node --check typesafe-doc-parser/web/app.js && python3 -m http.server 8130 --directory typesafe-doc-parser/web & sleep 2; curl -sf localhost:8130/index.html | grep -c "aria-live\|details\|Where should we begin"; grep -rEi "TYPESAFE_API_KEY|openai|anthropic" typesafe-doc-parser/web && exit 1 || echo FRONTEND-CLEAN; kill %1</verify>
  <done>Chat UI serves: hero + first message + chips on load; JS parses; no keys/LLM refs; thread/doubt/states all implemented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Chat website for the Jev-only doc parser (dark hero UI, thread, doubt-handling, honest states)</what-built>
  <how-to-verify>
    1. Start backend: TYPESAFE_API_KEY=xxx uvicorn typesafe-doc-parser.backend.api.server:app --port 8123
    2. Serve web: python3 -m http.server 8130 --directory typesafe-doc-parser/web, visit http://localhost:8130 — expect dark hero, first message, 3 chips
    3. Click a chip or ask "What is a System One model?" — expect user msg + green badge + quoted extract + Sources expander + copy ✓
    4. Ask "What is the holiday policy?" — expect red not-in-docs + closest section + rephrase chips, no fake answer
    5. Ask "When will Jev support image inputs?" — expect amber partial + clarifying question + did-you-mean chips; click one, thread continues
    6. Reload mid-thread — thread restores; check mobile width — composer docked, readable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
DOC-04 frontend slice: no-LLM grep clean, key server-side only, abstain/partial visually distinct from answers, doubt flows never dead-end, a11y basics (live region, focus, contrast, reduced-motion) present.
</verification>

<success_criteria>
Human confirms: hero looks like reference; first message short with working chips; answers cited with marks; doubt asked conversationally with chips; errors recoverable; no generated prose anywhere
</success_criteria>

<output>
After completion, create `typesafe-doc-parser/plans/01-03-SUMMARY.md`
</output>
