---
phase: 01-jev-doc-parser
plan: 2
type: execute
wave: 2
depends_on: [01-01]
files_modified: [typesafe-doc-parser/backend/search/jev_search.py, typesafe-doc-parser/backend/search/tune.py, typesafe-doc-parser/backend/api/server.py]
autonomous: true
requirements: [DOC-02]
user_setup:
  - service: typesafe
    why: "Jev API calls need a key; executor cannot create one"
    env_vars:
      - name: TYPESAFE_API_KEY
        source: "TypeSafe console -> API keys"

must_haves:
  truths:
    - "A docs question returns verbatim blocks ranked by Jev with per-block scores"
    - "A question with no docs answer returns abstain (not top-ranked irrelevant block)"
    - "No LLM library or generation call exists anywhere in the backend"
  artifacts:
    - path: "typesafe-doc-parser/backend/search/jev_search.py"
      provides: "BM25 shortlist + Choice where + Noul exists + verdict"
      min_lines: 120
    - path: "typesafe-doc-parser/backend/api/server.py"
      provides: "POST /ask returning extracts + marks JSON"
      min_lines: 60
    - path: "typesafe-doc-parser/backend/search/tune.py"
      provides: "Threshold tuning on gold queries"
  key_links:
    - from: "typesafe-doc-parser/backend/search/jev_search.py"
      to: "typesafe_sdk.TypeSafeClient.system_one"
      via: "single request with where + exists (+router) questions"
      pattern: "system_one"
    - from: "typesafe-doc-parser/backend/api/server.py"
      to: "typesafe-doc-parser/backend/search/jev_search.py"
      via: "import find()"
      pattern: "from.*jev_search import|import jev_search"
---

<objective>
Build the Jev-only search backend: no LLM, extractive answers with marks.

Purpose: This is the core proof — semantic_find + rerank patterns against our corpus.
Output: jev_search.py + server.py + tune.py
</objective>

<execution_context>
@/Users/sarnavo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/sarnavo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@typesafe-doc-parser/00-FEASIBILITY.md
@typesafe-doc-parser/backend/corpus/build_corpus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jev search core (BM25 + Choice where + Noul exists, one call)</name>
  <files>typesafe-doc-parser/backend/search/jev_search.py</files>
  <action>Using typesafe-sdk (pip install "typesafe-sdk>=0.5.7" --extra-index-url https://pypi.typesafe.ai/) and stdlib-only BM25 (implement ~40-line BM25 in-file; DO NOT add sklearn/embeddings/transformers/openai/anthropic deps): implement find(query, model="jev-latest"): 1) BM25 over corpus.json blocks trims to top 30 candidates; 2) if candidates<=200 use Choice instructions='Which block answers: "QUERY"?' criteria={block_id: None} (semantic_find: descriptions None because text is in state); 3) ALWAYS include Noul exists instructions='Does any supplied block address or answer: "QUERY"?' criteria true="At least one block states or directly implies the answer" false="No block addresses this"; 4) include section-router Choice over candidate page_titles in the SAME system_one call (state sent once: {query, blocks:[{id,heading_path,text}]}); 5) verdict(): exists>=0.7 answered, <0.35 not-in-docs, else partial. Return {verdict, exists, ranked:[{block_id, prob, text, heading_path, page_url}], router}. Two-pass: if a page has >200 blocks, Choice-1 picks window_id first (from corpus windows), then rank inside. NEVER generate prose — only copy block text.</action>
  <verify>TYPESAFE_API_KEY=xxx python3 -c "from typesafe-doc-parser.backend.search.jev_search import find; r=find('What is a System One model?'); print(r['verdict'], r['exists']); assert r['verdict']=='answered in this document' or r['exists']>=0.7; assert r['ranked'][0]['text']" ; TYPESAFE_API_KEY=xxx python3 -c "from typesafe-doc-parser.backend.search.jev_search import find; r=find('What is the company holiday policy?'); print(r['verdict'], r['exists']); assert r['verdict']=='not in this document', r"</verify>
  <done>Answerable query returns verdict answered with verbatim block; unanswerable query abstains (does not present top-rank as answer)</done>
</task>

<task type="auto">
  <name>Task 2: Threshold tuning script + no-LLM audit</name>
  <files>typesafe-doc-parser/backend/search/tune.py</files>
  <action>Write tune.py with 12 gold queries (6 answerable w/ expected block_id, 4 unanswerable, 2 partial) against our corpus; run find() on each, print exists/rank table, report accuracy of verdict at FOUND=0.7/ABSENT=0.35 and suggest adjustments. Also implement audit: grep backend/ for banned imports (openai, anthropic, transformers, sentence_transformers, langchain, llama) and fail with nonzero exit if found. Thresholds live in jev_search.py module constants FOUND/ABSENT (not hardcoded inline).</action>
  <verify>TYPESAFE_API_KEY=xxx python3 typesafe-doc-parser/backend/search/tune.py && python3 -c "import subprocess; subprocess.run(['grep','-rEi','openai|anthropic|transformers|sentence_transformers|langchain|llama_index','typesafe-doc-parser/backend'],check=False)" && echo AUDIT-DONE</verify>
  <done>Tuning table prints, verdict accuracy >=10/12, no banned LLM imports in backend</done>
</task>

<task type="auto">
  <name>Task 3: HTTP API exposing extracts + marks</name>
  <files>typesafe-doc-parser/backend/api/server.py</files>
  <action>FastAPI (or stdlib http.server if FastAPI unavailable) with POST /ask {query} -> {verdict, exists, router:{choice,confidence}, results:[{block_id, prob, heading_path, page_url, text}], usage}. GET /health. Read TYPESAFE_API_KEY from env server-side only. CORS allow local web/ origin. No key ever echoed in responses. Input: query 3-500 chars, reject empty.</action>
  <verify>uvicorn typesafe-doc-parser.backend.api.server:app --port 8123 & sleep 4; curl -sf localhost:8123/health; curl -sf -X POST localhost:8123/ask -H 'Content-Type: application/json' -d '{"query":"What is Jev?"}' | python3 -c "import json,sys; r=json.load(sys.stdin); assert 'verdict' in r and 'results' in r and r['results'][0]['text']; print(r['verdict'])"; kill %1</verify>
  <done>POST /ask returns verdict + ranked verbatim blocks + router choice with confidence</done>
</task>

</tasks>

<verification>
Cookbook parity: answerable/partial/unanswerable behavior matches semantic_find semantics (rank tells where, exists tells whether). Cost check: one /ask = 1 system_one call for the rank path (+N per-candidate calls only on the rerank path); log usage tokens per response.
</verification>

<success_criteria>
Jev-only backend returns verbatim docs extracts with exists verdict and per-block probabilities; abstains correctly; zero LLM dependencies; HTTP API serves it
</success_criteria>

<output>
After completion, create `typesafe-doc-parser/plans/01-02-SUMMARY.md`
</output>
