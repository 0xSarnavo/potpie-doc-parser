---
phase: 01-jev-doc-parser
plan: 4
type: execute
wave: 2
depends_on: [01-02]
files_modified: [typesafe-doc-parser/backend/search/jev_search.py, typesafe-doc-parser/backend/search/tune.py]
autonomous: true
requirements: [DOC-02, DOC-04]
user_setup:
  - service: typesafe
    why: "Tuning runs live Jev calls; executor cannot create a key"
    env_vars:
      - name: TYPESAFE_API_KEY
        source: "Already provisioned for this project"

must_haves:
  truths:
    - "Partially-covered questions land in the partial band instead of answered"
    - "Previously passing answerable (6/6) and unanswerable (4/4) behavior is unchanged"
    - "One /ask is still exactly one system_one call with zero new dependencies"
  artifacts:
    - path: "typesafe-doc-parser/backend/search/jev_search.py"
      provides: "exists + fully Nouls with three-way verdict logic"
    - path: "typesafe-doc-parser/backend/search/tune.py"
      provides: "Corrected golds incl. genuinely-partial queries, accuracy gate"
  key_links:
    - from: "typesafe-doc-parser/backend/search/jev_search.py"
      to: "typesafe_sdk.TypeSafeClient.system_one"
      via: "single request with where + exists + fully + router"
      pattern: "system_one"
---

<objective>
Fine-tune the Jev backend so the partial band actually fires. No new deps, no extra calls.

Purpose: Tune run is 10/12 — both misses are partial-labeled queries scoring answered at 0.94/0.97. Diagnosis: the single `exists` Noul asks "address OR answer", so topical coverage reads as answered. Fix with one companion Noul in the same call.
Output: updated jev_search.py + tune.py
</objective>

<execution_context>
@/Users/sarnavo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/sarnavo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@typesafe-doc-parser/00-FEASIBILITY.md
@typesafe-doc-parser/backend/search/jev_search.py
@typesafe-doc-parser/backend/search/tune.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fully-answers Noul + fix partial golds, re-tune</name>
  <files>typesafe-doc-parser/backend/search/jev_search.py, typesafe-doc-parser/backend/search/tune.py</files>
  <action>Minimal change only: 1) In _questions_for add ONE Noul `fully` (same system_one call, zero extra calls): instructions='Does a supplied block state the complete, direct answer to: "QUERY"?' true="A block gives the full answer with nothing material missing" false="Blocks only touch the topic or answer part of it". 2) verdict(exists, fully): answered iff fully>=FOUND; absent iff exists<ABSENT; else partial. Keep FOUND/ABSENT as module constants. 3) In tune.py replace the 2 mislabeled partials with genuinely-partial queries (topic touched, answer incomplete — e.g. uptime SLA, pricing/rate-limit specifics, image-input roadmap) and keep 2 old ones only if relabeled with documented reason; gate stays >=10/12 but target 12/12. Live-call budget for this task: max ~25 Jev calls (tune runs + a few probes). No new imports beyond stdlib+typesafe_sdk.</action>
  <verify>TYPESAFE_API_KEY=xxx python3 typesafe-doc-parser/backend/search/tune.py (expect verdict accuracy 12/12, or >=11/12 with per-miss reason in output); TYPESAFE_API_KEY=xxx python3 -c "from typesafe-doc-parser.backend.search.jev_search import find; r=find('What is the company holiday policy?'); assert r['verdict']=='not in this document', r"</verify>
  <done>Tune accuracy 12/12 (or documented >=11/12), unanswerable still abstains, partial band fires on genuinely-partial queries</done>
</task>

<task type="auto">
  <name>Task 2: Regression lock + audit + summary</name>
  <files>typesafe-doc-parser/backend/search/tune.py, typesafe-doc-parser/plans/01-04-SUMMARY.md</files>
  <action>Confirm no regressions: all 6 original answerables still answered with same top-1 blocks, all 4 unanswerables still absent; confirm still exactly 1 system_one call per /ask on rank path (code inspection + usage in one probe response); re-run import audit (must be clean); record final FOUND/ABSENT, per-/ask token usage, total live calls spent. Write 01-04-SUMMARY.md.</action>
  <verify>grep -rE '^(import|from) (openai|anthropic|transformers|sentence_transformers|langchain|llama_index)' typesafe-doc-parser/backend && exit 1 || echo AUDIT-CLEAN; TYPESAFE_API_KEY=xxx python3 -c "from typesafe-doc-parser.backend.search.jev_search import find; r=find('What is Jev?'); print(r['verdict'], r['exists'], r['usage'])"</verify>
  <done>Regression holds, audit clean, SUMMARY written with final thresholds + costs</done>
</task>

</tasks>

<verification>
Partial queries show amber verdict with mid exists + low fully; answerable/unanswerable behavior identical to 01-02 verification; spend for this plan <= ~30 live Jev calls total.
</verification>

<success_criteria>
Partial band demonstrably fires, tune gate passes, still 1 call per ask, zero new deps, audit clean
</success_criteria>

<output>
After completion, create `typesafe-doc-parser/plans/01-04-SUMMARY.md`
</output>
